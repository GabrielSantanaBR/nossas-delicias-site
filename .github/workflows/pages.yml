name: Validate and deploy static site

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' file; do
            echo "Checking $file"
            node --check "$file"
          done < <(find . -type f -name '*.js' -not -path './node_modules/*' -print0)

      - name: Validate production assets and metadata
        shell: bash
        run: |
          set -euo pipefail
          test -f index.html
          test -f styles.css
          test -f script.js
          test -f favicon.png
          test -f logo-nossas-delicias.svg
          test -f manifest.webmanifest
          test -f robots.txt
          test -f sitemap.xml
          test -f 404.html
          test -f v6/v12-production.css
          test -f v6/v12-production.js
          test -f v6/v15-cinematic.css
          test -f v6/v15-cinematic.js
          test -f v6/v15-admin.css
          test -f v6/v16-platform.css
          test -f v6/v16-platform.js
          python -m json.tool manifest.webmanifest >/dev/null
          python - <<'PY'
          import pathlib, xml.etree.ElementTree as ET
          ET.parse('sitemap.xml')
          html = pathlib.Path('index.html').read_text(encoding='utf-8')
          loader = pathlib.Path('script.js').read_text(encoding='utf-8')
          required_html = [
              'rel="canonical"',
              'og:title',
              'application/ld+json',
              'manifest.webmanifest?v=16',
              'script.js?v=16',
              'v12-production.css?v=16',
              'v15-cinematic.css?v=16',
              'v16-platform.css?v=16',
              'href="#sistema"',
          ]
          required_loader = [
              'v6/v16-platform.css?v=16',
              'v6/v16-platform.js?v=16',
          ]
          missing = [item for item in required_html if item not in html]
          missing += [item for item in required_loader if item not in loader]
          if missing:
              raise SystemExit(f'Missing v16 production assets/markers: {missing}')
          print('v16 HTML and loader markers OK')
          PY

  deploy:
    needs: validate
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
