name: Validate and deploy static site

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' file; do
            echo "Checking $file"
            node --check "$file"
          done < <(find . -type f -name '*.js' -not -path './node_modules/*' -print0)

      - name: Validate production assets and metadata
        shell: bash
        run: |
          set -euo pipefail
          test -f index.html
          test -f styles.css
          test -f script.js
          test -f favicon.png
          test -f logo-nossas-delicias.svg
          test -f manifest.webmanifest
          test -f robots.txt
          test -f sitemap.xml
          test -f 404.html
          test -f v6/v12-production.css
          test -f v6/v12-production.js
          test -f v6/v15-cinematic.css
          test -f v6/v17-public.css
          test -f v6/v17-public.js
          python -m json.tool manifest.webmanifest >/dev/null
          python - <<'PY'
          import pathlib, xml.etree.ElementTree as ET
          ET.parse('sitemap.xml')
          html = pathlib.Path('index.html').read_text(encoding='utf-8')
          loader = pathlib.Path('script.js').read_text(encoding='utf-8')
          required_html = [
              'rel="canonical"',
              'og:title',
              'application/ld+json',
              'manifest.webmanifest?v=17',
              'script.js?v=17',
              'v17-public.css?v=17',
              'href="#portfolio"',
          ]
          required_loader = [
              'v6/v17-public.css?v=17',
              'v6/v17-public.js?v=17',
          ]
          forbidden_public = [
              'href="#sistema"',
              'v6/v16-platform.js?v=17',
              'v6/v6-admin-core.js?v=17',
              'id="admin-fab"',
          ]
          missing = [item for item in required_html if item not in html]
          missing += [item for item in required_loader if item not in loader]
          leaked = [item for item in forbidden_public if item in html or item in loader]
          if missing:
              raise SystemExit(f'Missing v17 public assets/markers: {missing}')
          if leaked:
              raise SystemExit(f'Private-system markers leaked into public bundle: {leaked}')
          print('v17 public/private separation markers OK')
          PY

  deploy:
    needs: validate
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
